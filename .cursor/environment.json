{
  "name": "DeusCFO",
  "install": "sudo apt-get update && sudo apt-get install -y python3.12-venv && python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/python -m pip install -r backend/requirements.txt pytest ruff && npm ci --prefix frontend && npm run build --prefix frontend",
  "start": ".venv/bin/python deuscfo.py dev"
}
